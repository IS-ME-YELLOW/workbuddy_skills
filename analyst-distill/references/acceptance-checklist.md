# 蒸馏产出验收清单（Acceptance Checklist）

> **定位**：本清单是 analyst-distill 的质量判据，**领域无关**——适用于宏观/策略/固收/金工/行业/买方/机构等任意分析师类型。
> 它不是"建议"，是**每份产出可逐条勾选的验收标准**：全部通过才算该 phase 完成；未过项必须修复，或在该条目旁写明"不适用/放宽理由"。
> 数值下限（如框架 ≥8、指标 ≥20）为从已有三完成品反推的**参考值**，若目标分析师方法论体量明显不同，可调整并在验收时注明理由，不得静默偏离。

---

## 图例说明

- ✅ = 必须满足（不满足即返工）
- ⚠️ = 参考基准（以三完成品为默认值，可附理由调整）
- 来源标注三色纪律贯穿所有文档：✅ 原文 / ⚠️ 外推 / ❌ 推断（详见 §9-1）

---

## §1 成品 SKILL.md（Phase 3 产出）

- [ ] ✅ `name` 符合 `analyst-{specialty}-{name}`（specialty ∈ macro/strategy/fixed-income/quant，或按实际扩展）
- [ ] ✅ frontmatter 仅含 `name` 与 `description`；`description` 使用第三人称 + 触发词 + 一句话定位
- [ ] ✅ `description` **无尖括号 `<` `>`**（package_skill.py 校验硬性失败项；阈值一律写成"高于/低于 X%"）
- [ ] ✅ 含五章节：使用流程 / 触发场景速查表 / 数据获取 / 脚本使用 / 红线与注意事项
- [ ] ✅ 使用流程第一步是"读 `assets/views.md` 最新观点"（观点时效性第一原则）
- [ ] ⚠️ 工作流明确"先体检→再择时→后配置/结构"的顺序（与 decision-rules 一致）
- [ ] ⚠️ 红线节含：机构归属变化警示（分析师跳槽/离职）、观点时效性警示、与同领域其他已蒸馏技能差异表
- [ ] ⚠️ 数据获取节列出可用数据源（westock-data/westock-tool/neodata 或平台降级方案 §domain-adapters）

## §2 references/framework.md（Phase 2 产出）

- [ ] ✅ 顶部有来源标注图例（✅/⚠️/❌ 定义）
- [ ] ✅ 框架总览按分析师自己的方法论主体分层（参考：宏观"总量→结构→择时→配置"、策略"主要矛盾→资产定价→结构主线"），**不得强套**其他领域分层
- [ ] ✅ 每个框架含四要素：**定义**（是什么）/ **逻辑**（为什么）/ **选股或配置应用**（怎么用）/ **窗口内实例**（带时点标注）
- [ ] ✅ 每个阈值、权重、参数逐条带 ✅/⚠️/❌ 标注；❌ 值写明"最小假设，可替换"
- [ ] ✅ 时点观点（"2026.08 看多"类）不混入规则主体，实例标注时点并指向 views.md
- [ ] ✅ 末尾有**场景调用速查表**（遇到什么市场状态→调哪个框架）
- [ ] ⚠️ 框架数 ≥ 8（覆盖方法论主体为准，体量小可降至 6 并注明理由）

## §3 references/indicators.md（Phase 2 产出）

- [ ] ✅ 每个指标含**六字段**：编号 / 指标名 / 口径（定义与单位）/ 频率 / 所属框架 / 使用方式与阈值
- [ ] ✅ 指标按功能分类组织（参考：估值风险溢价 / 资金流情绪 / 信用基本面 / 海外利率汇率 / 市场结构主题）
- [ ] ✅ 每个阈值带来源标注（✅/⚠️/❌），❌ 写明"最小假设"
- [ ] ✅ 末尾附**跟踪优先级分级**（哪些必跟踪、哪些按需、哪些仅研究用）
- [ ] ⚠️ 指标数 ≥ 20

## §4 references/decision-rules.md（Phase 2 产出）

- [ ] ✅ **规则 0（信号分级与总原则）** 存在：定义信号强弱等级、触发含义、与仓位/操作建议的映射
- [ ] ✅ `decision-rules.md` 规则准入符合至少一项：多篇报告/访谈/路演反复使用；分析师明确命名为框架/模型/指标体系/回测规则；年度/中期策略中的框架级更新；或服务于上述框架的工程化代理（必须标 ❌ 推断）
- [ ] ✅ 每条规则含：**触发条件（可计算）→ 计算步骤 → 量化阈值 → 信号输出**
- [ ] ✅ 阈值全部量化到"可直接翻译为脚本"的程度（数字、比较符、窗口长度、频率）
- [ ] ✅ 末尾有 **S 信号触发汇总表**（编号/信号名/触发条件/强度），并标注"信号工程化编号系本技能设计"
- [ ] ✅ 附**数据-规则映射附录**（每条规则用到哪些数据字段）
- [ ] ✅ 时点观点不进规则；规则区只留"观点更新模板"结构
- [ ] ✅ 规则条数 ≥ 6 且覆盖方法论主体（参考：三完成品均为 11-12 条）

## §5 assets/profile.md（Phase 3 产出）

- [ ] ✅ 含四章节：分析师档案（任职经历/执业信息）/ 方法论特征 / 窗口内观点验证记录 / 风险与注意事项
- [ ] ✅ **机构归属变化红线段**存在（任职机构、起止时间、当前身份；引用观点时标注任职期归属）
- [ ] ✅ 观点验证记录为表格式：时间 / 观点 / 后续验证（✅⏳❓⚠️ 分级）/ 备注
- [ ] ✅ Phase 4 完成后新增"信号验证状态"节（哪些 S 信号最高置信、哪些待样本累积）

## §6 assets/views.md（Phase 3 产出）

- [ ] ✅ 六要素齐全：顶部警示语（"使用前核对最新观点"）/ 时间倒序 / 当前有效 vs 历史参考分区 / 旧快照标"已过期" / 每条含时点与出处 / 演化主线总结
- [ ] ✅ 当前有效区与最新观点保持同步（Phase 1 末端检索 + 使用前核对）
- [ ] ✅ 观点与决策规则物理分离（views 只存观点，不进规则）；明确 `views.md` 是观点时间线/口径校准层，不是决策规则层，不得替代 `decision-rules.md` 或 `screen.py`

## §7 assets/validation.md（Phase 4 产出）

- [ ] ✅ **五段式结构**：数据口径说明（三层来源表）→ 事件时点复算对照表 → 实时信号快照 → 验证结论（含局限）→ 口径差异双处标注
- [ ] ✅ 三层来源清单明确：✅westock 直连 / ⚠️观点锚点+插值 / ❌工程代理，**每个字段都归类**
- [ ] ✅ 事件对照表：≥5 个关键事件时点 ×（复算信号 / 分析师同期观点 / 前瞻 1M-3M 真实行情）三方对照
- [ ] ✅ 误报点如实记录并归因（数据口径差异 / 框架边界（必要非充分）/ 原文含糊）
- [ ] ✅ 结论写明：**绩效数字 ≠ 分析师框架的绩效**，❌ 推断参数是主要不确定来源
- [ ] ✅ 口径差异在 `_meta` 与 validation.md **双处**标注（例：不同数据源绝对水平背离，仅分位/方向可用）

## §8 scripts/screen.py（Phase 3 产出）

- [ ] ✅ **纯标准库**（无 pandas/numpy 依赖；滤波可用共轭梯度等自实现）
- [ ] ✅ **五函数结构**：计算函数（每信号一个）/ `aggregate_signals` / `render_report` / `demo_data` / `main`
- [ ] ✅ 顶部**参数区注释块**：每个常量列出默认值与来源分级（✅/⚠️/❌），与 decision-rules.md 图例一致
- [ ] ✅ 三种运行入口：`--data <json>` 真实数据 / 默认演示模式（输出明确标注"合成数据"）/ `--json-out`（布尔标志，stdout 输出机器可读 JSON）
- [ ] ✅ 输入 JSON：`history` 为"字段→月份数组"转置结构（最新在末尾），`extras` 为风格/阶段类单值，`_meta` 为数据来源说明
- [ ] ✅ 输出 JSON 含：`signals`（id/name/triggered/detail/strength）/ `positions` / `extras` / `n_triggered` / `total`
- [ ] ✅ 演示模式可跑通（无数据输入也能展示全流程输出）
- [ ] ✅ A 股惯例：涨红跌绿、货币 ¥
- [ ] ✅ `--data` 输入校验（缺字段给出明确报错而非静默出错）

## §9 全局（贯穿所有 phase）

- [ ] ✅ **每个 phase 向用户汇报并等确认**（用户明示"免确认直接跑"除外）
- [ ] ✅ **读取顺序 ≠ 决策优先级**：先读 `views.md` 顶部最新观点是为了校准日期、机构与有效期；真正决策以 `decision-rules.md`/`screen.py` 当前信号为主
- [ ] ✅ **观点与规则严格分离**：信号计算不依赖观点快照；若最新观点与信号冲突，必须输出分歧说明。✅原文规则 + 最新真实数据的高置信信号优先；主要依赖 ❌推断参数或缺失数据代理的信号不得机械压过 views，只能标为低置信分歧
- [ ] ✅ **来源标注纪律**：一切阈值/权重/参数/回测数字带 ✅/⚠️/❌；回测数字加"~"并注"取自公开报告，以原文为准"；观点实例具体小数不得杜撰，写"以当期报告原文为准"
- [ ] ✅ **禁止虚假精确**：无法核对到原文的数字一律标"推断"，不得写成引用
- [ ] ✅ 产出文档结构齐全（references×3 + assets×3 + SKILL.md + scripts/screen.py）
- [ ] ✅ 用 `package_skill.py` 校验通过（"Skill is valid"）；无该校验工具时按 §1/§9 人工逐条勾选替代
- [ ] ✅ zip 打包与最新产出同步（含 validation.md）
- [ ] ✅ 每 phase 完成写入 `.workbuddy/memory/YYYY-MM-DD.md`

---

## 使用方式

1. Phase 2 产出后 → 勾选 §2/§3/§4 + §9 相关项
2. Phase 3 产出后 → 勾选 §1/§5/§6/§8 + §9 相关项
3. Phase 4 产出后 → 勾选 §7 + §9 相关项
4. 全部完成 → 整体过一遍 §9，未过项当场修复
5. 勾选结果写入该技能 `assets/validation.md` 附录或单独自查记录，随 zip 交付
